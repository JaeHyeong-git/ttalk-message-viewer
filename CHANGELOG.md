# 변경 이력

이 문서는 TTalk 쪽지 뷰어의 버전별 변경 내역을 기록합니다.

---

## [v1.3] - 2026-05-18

### 추가
- **텍스트 붙여넣기로 쪽지 추가**: 사내 보안 정책으로 브라우저 파일 업로드가 차단되는 환경을 대비해, txt 파일 내용을 직접 복사·붙여넣어 쪽지를 저장하는 기능을 추가했습니다.
  - "📥 쪽지 추가" 모달이 "📁 파일로 추가" / "📋 텍스트 붙여넣기" 두 탭으로 구성됩니다.
  - 붙여넣기 탭에서는 해당 내용이 받은 쪽지인지 보낸 쪽지인지 직접 선택한 후, 텍스트 영역에 붙여넣고 "쪽지 저장" 버튼으로 저장합니다.
  - 붙여넣은 내용도 파일 업로드와 동일한 파서를 거치며, 중복 제거·사번 제거·인용 복원이 모두 동일하게 적용됩니다.
  - 입력 글자 수가 실시간으로 표시됩니다.

### 변경
- 헤더의 "📁 파일 추가" 버튼이 "📥 쪽지 추가"로 변경되었습니다 (파일/붙여넣기 두 방식을 모두 포괄).
- 업로드 내역 모달에서 붙여넣기로 추가한 항목은 📋 아이콘으로 구분 표시됩니다.
- 파일 드래그앤드롭 기능은 그대로 유지됩니다 (파일 업로드가 허용되는 환경에서는 기존 방식 계속 사용 가능).

---

## [v1.2] - 2026-05-18

### 추가
- **별표 기능**: 각 쪽지 말풍선에 ⭐ 버튼이 추가되어, 자주 보고싶은 쪽지를 별표할 수 있습니다. 별표된 쪽지는 사이드바의 "⭐ 저장한 쪽지" 탭에서 모아볼 수 있고, 클릭하면 원본 대화 위치로 이동하면서 해당 쪽지가 잠시 강조됩니다.
- **사이드바 탭**: 사이드바 상단에 "💬 대화" / "⭐ 저장한 쪽지" 두 탭이 생겼습니다. 각 탭 옆에 개수 뱃지가 표시됩니다.
- **키워드 저장 기능**: 자주 검색하는 키워드를 미리 저장해두고, 사이드바의 "🔖 저장한 검색어" 영역에서 칩을 클릭하면 즉시 검색됩니다. 칩 옆 × 버튼으로 개별 삭제 가능합니다.
- **키워드 추가 모달**: "+ 추가" 버튼으로 새 검색어를 입력할 수 있는 모달이 열립니다. 현재 검색창에 입력된 텍스트가 있으면 자동으로 채워집니다. Enter 키로 빠른 저장 가능.
- **키워드 섹션 접기/펴기**: 저장된 검색어가 많아져도 사이드바를 깔끔하게 유지할 수 있도록 섹션을 접을 수 있습니다.

### 변경
- IndexedDB 스키마 v2: `keywords` store가 추가되어 저장된 검색어가 영구 보관됩니다. `messages` store의 각 쪽지 객체에는 `starred` 플래그가 함께 저장됩니다.
- "전체 삭제" 시 별표 정보와 저장된 검색어도 함께 삭제됩니다.

### 호환성
- v1.1에서 저장된 쪽지 데이터는 그대로 유지되며, 별도 마이그레이션 없이 사용 가능합니다. 별표는 새로 지정해야 합니다.

---

## [v1.1] - 2026-05-18

### 추가
- **업로드 경과일 표시**: 상단 헤더에 가장 최근 업로드 시점과 경과일을 표시 (예: "최근 업로드: 2026.05.18. (3일 전)").
- **20일 초과 경고 배너**: 마지막 업로드 후 20일이 경과하면 헤더 아래에 주황 그라데이션 경고 배너를 표시하여 백업 갱신을 유도. 부드러운 색상 애니메이션으로 자연스럽게 주의를 끌도록 처리.

### 변경
- 파일 추가 시 IndexedDB의 `files` 스토어에 `uploadedAt` 타임스탬프를 함께 저장. 같은 파일을 다시 올리면 시각이 갱신되어 최신 업로드 시점을 추적.

### 안내
- v1.0에서 저장된 파일에는 `uploadedAt` 정보가 없으므로 경과일 계산에서 제외됩니다. v1.1로 업그레이드 후 파일을 한 번 다시 추가하면 그때부터 정확히 추적됩니다.

---

## [v1.0] - 2026-05-15

### 추가
- **단일 HTML 파일 뷰어**: 설치 없이 브라우저에서 더블클릭만으로 실행되는 단일 HTML 도구. 사내 PC에서 `.exe` 실행이 어려운 환경을 고려한 설계.
- **TTalk 백업 파서**: 받은쪽지함/보낸쪽지함 txt 파일을 파싱하여 발신자, 수신자, 시각, 제목, 본문, 첨부파일을 분리 추출.
  - **신구 포맷 모두 지원**: 블록 구분자가 하이픈(`---...---`)인 구포맷과 등호+끝(`===...===끝.`)인 신포맷 모두 인식.
  - **`내용:` 라벨 유무 모두 처리**: 구포맷의 `내용:` 라벨, 신포맷의 라벨 없는 본문 시작 모두 정확히 추출.
  - **`수신:` / `참조:` 헤더 파싱**: 메인 블록의 수신자/참조자 정보를 직접 추출. 복수 수신자(쉼표 구분)도 지원.
  - **사번 자동 제거**: `유재형(02112801)` 같은 이름에서 괄호 안 사번을 자동 제거하여, 동일 인물이 한 명으로 인식되도록 처리.
- **인용 복원**: 답장 쪽지에 포함된 인용부에서 백업 누락된 상대방 쪽지를 자동 복원하여 표시. 인용부의 짧은 구분선이 15~39자 범위면 인식.
- **자동 중복 제거**: 발신자+수신자+시각+내용을 키로 같은 쪽지를 한 번만 표시.
- **복수 수신자 대화 그룹핑**: 한 쪽지에 여러 수신자가 있어도 발신자 또는 첫 번째 수신자 기준으로 통합 그룹핑.
- **카카오톡 스타일 UI**: 좌측 대화 상대 목록, 우측 채팅 말풍선 (내가 보낸 쪽지는 오른쪽 초록, 받은 쪽지는 왼쪽 회색).
- **IndexedDB 영구 저장**: 파싱된 쪽지를 브라우저 DB에 저장하여 HTML을 닫았다 열어도 데이터 유지.
- **실시간 검색**: 이름·제목·내용 검색 및 매칭 부분 노란색 하이라이트. `Ctrl+F` 단축키 지원.
- **CSV 내보내기**: 현재 검색/대화 조건의 쪽지를 엑셀 호환 CSV로 다운로드.
- **드래그앤드롭 업로드**: 화면 어디든 txt 파일을 끌어다 놓으면 자동 처리.
- **파일 추가 모달**: "📁 파일 추가" 버튼 클릭 시 모달이 열리며, 모달 내부에서도 드래그앤드롭과 파일 선택이 가능.
- **업로드 내역 모달**: "📋 업로드 내역" 버튼으로 업로드한 파일 목록을 받은쪽지/보낸쪽지 구분과 함께 표시.
- **내 이름 변경**: "나"로 인식할 이름을 사용자가 직접 설정 가능. DB에 저장되어 다음 세션에서도 유지.
- **전체 삭제**: IndexedDB의 모든 쪽지 및 파일 내역을 단일 트랜잭션으로 완전 삭제.

### 사용 환경
- 사내 PC에서 보안 규제로 `.exe` 실행이 어려운 환경을 위해 HTML 단일 파일로 동작.
- 데이터가 외부로 전송되지 않는 완전 로컬 도구.
